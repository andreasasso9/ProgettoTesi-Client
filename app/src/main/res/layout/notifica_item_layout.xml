<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/mainContainer"
    android:background="#B71C1C">

    <LinearLayout android:id="@+id/containerItem"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_weight="1"
        android:background="@color/white">

        <TextView android:id="@+id/sender"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="35dp"
            android:text="user"
            android:textStyle="bold" />

        <TextView android:id="@+id/descrizione_notifica"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="descrizione"
            android:textSize="25dp"
            android:maxLines="2"/>

        <ImageView android:id="@+id/foto_notifica"
            android:layout_width="50dp"
            android:layout_height="70dp"
            android:contentDescription="@null" />
    </LinearLayout>

    <TextView
        android:id="@+id/eliminaNotifica"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:maxLines="1"
        android:text="Elimina"
        android:textColor="@color/white"
        android:textSize="30dp"
        app:drawableStartCompat="@drawable/icons8_cestino_64"
        android:background="#B71C1C"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:padding="10dp"/>

</LinearLayout>